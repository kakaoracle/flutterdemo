import 'package:flutter/material.dart';
import 'package:kaka/demos/BlocApp.dart';

/* 
* main函数是主入口,其他具体页面可以在runApp中进行定义
* 且多语言,路由都是在自己的app页面中定义的
* 不同模块测试直接将runApp中的入参替换掉即可
* */

void main() {
  runApp(BlocApp());
}
